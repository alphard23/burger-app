<h1>Eat more burgers!</h1>

<h2>Available burgers!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <p>{{burger_name}}</p>
      <button class="btn btn-primary data-id="{{id}}>Eat this burger.</button>
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <p>{{burger_name}}</p>
    {{/if}}
  {{/each}}
</ul>

<h2>Eat a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>


  <button type="submit" id="submit-burger">Add Burger</button>
</form>
